# iris-sys-recs-2026
## Task-5

### Updating the Docker Compose
* Added a volume block to the mysql service mapping db_data to ```/var/lib/mysql```.
* Also added a global volume block to ensure persistence.
